apiVersion: v1
kind: Service
metadata:
  name: cart-db
  namespace: retail-store
spec:
  selector:
    app: cart-db
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP