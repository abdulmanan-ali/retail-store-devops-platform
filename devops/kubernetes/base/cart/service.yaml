#service.yaml
apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: retail-store 
spec:
  selector:
    app: cart
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP