upstream ui {
    server ui:8080;
}

server {
    listen 80;
    server_name localhost;

    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log;

    # All traffic goes to UI — UI calls backends internally over Docker network
    location / {
        proxy_pass http://ui;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}


# upstream ui {
#     server ui:8080;
# }

# upstream catalog {
#     server catalog:8080;
# }

# upstream cart {
#     server cart:8080;
# }

# upstream checkout {
#     server checkout:8080;
# }

# upstream orders {
#     server orders:8080;
# }

# server {
#     listen 80;
#     server_name localhost;

#     # Access and error logs
#     access_log /var/log/nginx/access.log;
#     error_log  /var/log/nginx/error.log;

#     # UI — catch-all, must be last
#     location / {
#         proxy_pass http://ui;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Catalog API
#     location /catalog/ {
#         proxy_pass http://catalog/;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Cart API
#     location /cart/ {
#         proxy_pass http://cart/;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Checkout API
#     location /checkout/ {
#         proxy_pass http://checkout/;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Orders API
#     location /orders/ {
#         proxy_pass http://orders/;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }